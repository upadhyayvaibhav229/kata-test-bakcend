# Copy to .env and fill in your values
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/karate_db"
PORT=4000
NODE_ENV=development

# Optional — set a secret for admin routes if you add auth later
# ADMIN_SECRET="your-secret-here"
